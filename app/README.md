# App Folder

The first working web app will live here.

Recommended first files:

- `index.html`
- `styles.css`
- `main.js`

The initial app should run directly in a browser without installing dependencies.
